import { hotDog } from 'marketplace/helpers/hot-dog';
import { module, test } from 'qunit';

module('Unit | Helper | hot dog');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hotDog([42]);
  assert.ok(result);
});
