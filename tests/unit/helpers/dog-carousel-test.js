import { dogCarousel } from 'marketplace/helpers/dog-carousel';
import { module, test } from 'qunit';

module('Unit | Helper | dog carousel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dogCarousel([42]);
  assert.ok(result);
});
