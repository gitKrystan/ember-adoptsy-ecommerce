<div class="modal-content">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title">{{dog.name}}</h4>
  </div>
  <div class="modal-header">
    {{dog-carousel dog}}
  </div>
  <div class="modal-body">
    {{dog.description}}
  </div>
  <div class="modal-footer">
    {{#if dog.inDoghouse}}
      <button {{action 'removeFromDoghouse' dog}} class="btn btn-warning" data-dismiss="modal">Sorry, I No Longer Want To Meet {{dog.name}}</button>
    {{else}}
      <button {{action 'addToDoghouse' dog}} class="btn btn-primary">I Want To Meet {{dog.name}}</button>
    {{/if}}
    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
  </div>
</div><!-- /.modal-content -->
